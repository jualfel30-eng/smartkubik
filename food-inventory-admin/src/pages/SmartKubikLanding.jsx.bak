import React, { useState, useEffect, useRef } from 'react';
import { Link } from 'react-router-dom';
import LightRays from '@/components/LightRays';

// Add keyframes for animations
const styleSheet = document.createElement('style');
styleSheet.textContent = `
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .animate-float {
    animation: float 8s ease-in-out infinite;
  }

  .animate-bounce-custom {
    animation: bounce 3s ease-in-out infinite;
  }

  .perspective-container {
    perspective: 1000px;
  }

  .glass {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(12px);
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(12px);
  }

  .glass-card:hover {
    border-color: rgba(251, 191, 36, 0.4) !important;
  }

  .rotate-y-12 {
    transform: rotateY(12deg) rotateX(6deg);
  }

  .hover-rotate-0:hover {
    transform: rotateY(0deg) rotateX(0deg) !important;
  }

  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.8;
    }
    50% {
      opacity: 1;
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }

  .bg-navy-900 {
    background-color: #0A0F1C;
  }

  .text-gradient {
    background: linear-gradient(to right, #06B6D4, #A855F7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes pulse-glow {
    0%, 100% {
      filter: drop-shadow(0 0 10px rgba(6, 182, 212, 0.3));
    }
    50% {
      filter: drop-shadow(0 0 20px rgba(168, 85, 247, 0.5));
    }
  }

  .animate-pulse-glow {
    animation: pulse-glow 3s ease-in-out infinite;
  }

  .stack-card {
    will-change: transform, opacity;
  }
`;
if (!document.querySelector('#landing-animations')) {
  styleSheet.id = 'landing-animations';
  document.head.appendChild(styleSheet);
}

const SmartKubikLanding = () => {
  const [language, setLanguage] = useState('es');
  const [isLargeScreen, setIsLargeScreen] = useState(false);

  // Helper function for translations
  const t = (es, en) => (language === 'es' ? es : en);

  const toggleLanguage = () => {
    setLanguage(prev => prev === 'es' ? 'en' : 'es');
  };

  useEffect(() => {
    const checkScreen = () => {
      setIsLargeScreen(window.innerWidth >= 1024);
    };

    checkScreen();
    window.addEventListener('resize', checkScreen);

    return () => window.removeEventListener('resize', checkScreen);
  }, []);

  // Scroll Stack Animation
  useEffect(() => {
    const section = document.querySelector('#section-scroll-stack');
    const cards = document.querySelectorAll('.stack-card');
    const title = document.querySelector('#stack-title');
    const footer = document.querySelector('#stack-footer');

    if (!section || !cards.length || !title || !footer) return;

    // Config
    const cardGap = 20; // Pixels between stacked cards
    const scaleStep = 0.05; // Scale difference per card

    // Cache dimensions to avoid layout thrashing
    let sectionTop = 0;
    let sectionHeight = 0;
    let scrollDistance = 0;
    let viewportHeight = window.innerHeight;

    function updateDimensions() {
      if (!section) return;
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
      const rect = section.getBoundingClientRect();
      const rectTop = rect.top + scrollTop;

      sectionTop = rectTop;
      sectionHeight = section.offsetHeight;
      scrollDistance = sectionHeight - viewportHeight;

      console.log('Stack Section Dimensions:', {
        sectionTop,
        sectionHeight,
        viewportHeight,
        scrollDistance
      });
    }

    // Initial calculation - use RAF to ensure DOM is ready
    requestAnimationFrame(() => {
      updateDimensions();
      handleScroll(); // Initial call after dimensions are set
    });

    const handleResize = () => {
      viewportHeight = window.innerHeight;
      updateDimensions();
    };

    window.addEventListener('resize', handleResize);
    window.addEventListener('load', updateDimensions);

    function handleScroll() {
      if (!section) return;

      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      // Calculate progress based on scroll position relative to section start
      let rawProgress = (scrollTop - sectionTop) / scrollDistance;

      // Clamp progress
      let progress = Math.max(0, Math.min(1, rawProgress));

      console.log('Scroll Progress:', {
        scrollTop,
        sectionTop,
        scrollDistance,
        rawProgress,
        progress
      });

      // Title Animation (Fade out instantly)
      if (progress > 0.0) {
        title.style.opacity = '0';
        title.style.transform = 'translateY(-20px)';
      } else {
        title.style.opacity = '1';
        title.style.transform = 'translateY(0)';
      }

      // Cards Logic
      cards.forEach((card, index) => {
        // Calculate specific trigger points for each card
        const cardStart = 0.0 + (index * 0.12);

        let cardProgress = (progress - cardStart) / 0.15;
        cardProgress = Math.max(0, Math.min(1, cardProgress));

        // Entrance: Translate Y from lower down to 0
        const easeOut = 1 - Math.pow(1 - cardProgress, 3);
        let translateY = (1 - easeOut) * (window.innerHeight * 0.55);

        // Stacking Logic
        const currentCardFloat = (progress - 0.0) / 0.12;
        let depth = Math.max(0, currentCardFloat - (index + 1));

        // Apply stacking effects based on depth
        let stackOffset = -(depth * cardGap);
        let scale = 1 - (depth * scaleStep);
        let opacity = 1 - (depth * 0.15);

        // Apply transforms
        const finalY = translateY + stackOffset;
        card.style.transform = `translate3d(0, ${finalY}px, 0) scale(${Math.max(0.8, scale)})`;
        card.style.opacity = Math.max(0, opacity);
      });

      // Footer Animation
      if (progress > 0.55) {
        footer.style.opacity = '1';
        footer.style.transform = 'translateY(0)';
      } else {
        footer.style.opacity = '0';
        footer.style.transform = 'translateY(40px)';
      }
    }

    // Optimize with RAF
    let ticking = false;
    const scrollListener = () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          handleScroll();
          ticking = false;
        });
        ticking = true;
      }
    };

    window.addEventListener('scroll', scrollListener, { passive: true });

    return () => {
      window.removeEventListener('scroll', scrollListener);
      window.removeEventListener('resize', handleResize);
      window.removeEventListener('load', updateDimensions);
    };
  }, []);

  return (
    <div style={{ backgroundColor: '#0A0F1C', color: '#F8FAFC', overflowX: 'hidden', fontFamily: 'Inter, sans-serif' }}>
      
      {/* Navigation */}
      <nav style={{ position: 'fixed', top: 0, width: '100%', zIndex: 50, padding: '1rem' }}>
        <div style={{ maxWidth: '80rem', margin: '0 auto' }}>
          <div style={{ 
            background: 'rgba(255, 255, 255, 0.03)',
            backdropFilter: 'blur(12px)',
            WebkitBackdropFilter: 'blur(12px)',
            border: '1px solid rgba(255, 255, 255, 0.08)',
            borderRadius: '9999px',
            padding: '0.75rem 1.5rem',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center'
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
              <img src="/assets/logo-smartkubik.png" alt="SmartKubik" style={{ height: '2rem' }} />
            </div>

            <div style={{ display: 'flex', alignItems: 'center', gap: '2rem', fontSize: '0.875rem', fontWeight: 500 }}>
              <Link to="/login" style={{ color: '#94A3B8', textDecoration: 'none', transition: 'color 0.3s' }}>
                {t('Inicia sesión', 'Log in')}
              </Link>
              <Link to="/register" style={{ 
                background: 'linear-gradient(135deg, #06B6D4, #10B981)',
                color: 'white',
                padding: '0.5rem 1.5rem',
                borderRadius: '9999px',
                fontWeight: 700,
                textDecoration: 'none'
              }}>
                {t('Regístrate', 'Sign Up')}
              </Link>
            </div>
          </div>
        </div>
      </nav>

      {/* SECTION 1: HERO */}
      <section style={{ 
        position: 'relative',
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        paddingTop: '5rem',
        overflow: 'hidden',
        background: 'radial-gradient(ellipse at 20% 30%, rgba(6, 182, 212, 0.15) 0%, transparent 50%), radial-gradient(ellipse at 80% 70%, rgba(16, 185, 129, 0.12) 0%, transparent 50%), linear-gradient(180deg, #0A0F1C 0%, #1E293B 100%)'
      }}>
        {/* Grid Overlay */}
        <div style={{ 
          position: 'absolute',
          inset: 0,
          backgroundSize: '50px 50px',
          backgroundImage: 'linear-gradient(to right, rgba(255, 255, 255, 0.05) 1px, transparent 1px), linear-gradient(to bottom, rgba(255, 255, 255, 0.05) 1px, transparent 1px)',
          opacity: 0.3,
          pointerEvents: 'none'
        }} />

        {/* Floating Orbs */}
        <div style={{ position: 'absolute', top: '25%', left: '25%', width: '24rem', height: '24rem', background: 'rgba(6, 182, 212, 0.2)', borderRadius: '50%', filter: 'blur(128px)', animation: 'pulse 2s infinite' }} />
        <div style={{ position: 'absolute', bottom: '25%', right: '25%', width: '24rem', height: '24rem', background: 'rgba(16, 185, 129, 0.2)', borderRadius: '50%', filter: 'blur(128px)', animation: 'pulse 2s infinite', animationDelay: '1s' }} />

        <div style={{ maxWidth: '80rem', margin: '0 auto', padding: '0 1.5rem', position: 'relative', zIndex: 10, width: '100%' }}>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(min(100%, 500px), 1fr))', gap: '3rem', alignItems: 'center' }}>
            
            {/* Hero Content */}
            <div style={{ textAlign: 'left', display: 'flex', flexDirection: 'column', gap: '2rem' }}>
              
              {/* Badge */}
              <div style={{ 
                display: 'inline-flex',
                alignItems: 'center',
                gap: '0.5rem',
                padding: '0.5rem 1rem',
                borderRadius: '9999px',
                background: 'rgba(255, 255, 255, 0.03)',
                border: '1px solid rgba(6, 182, 212, 0.3)',
                width: 'fit-content'
              }}>
                <span style={{ position: 'relative', display: 'flex', height: '0.75rem', width: '0.75rem' }}>
                  <span style={{ animation: 'ping 1s infinite', position: 'absolute', height: '100%', width: '100%', borderRadius: '50%', background: '#06B6D4', opacity: 0.75 }} />
                  <span style={{ position: 'relative', borderRadius: '50%', height: '0.75rem', width: '0.75rem', background: '#06B6D4' }} />
                </span>
                <span style={{ color: '#22D3EE', fontSize: '0.875rem', fontWeight: 500, letterSpacing: '0.05em', textTransform: 'uppercase' }}>
                  {t('Nueva Generación ERP 2026', 'Next Gen ERP 2026')}
                </span>
              </div>

              {/* Headline */}
              <h1 style={{ fontSize: 'clamp(2.5rem, 5vw, 4.5rem)', fontWeight: 800, lineHeight: 1.1, letterSpacing: '-0.02em' }}>
                {language === 'es' ? (
                  <>
                    Deja de Administrar <span style={{ background: 'linear-gradient(to right, #EF4444, #F59E0B)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>Caos</span>.<br/>
                    Empieza a Administrar <span style={{ background: 'linear-gradient(135deg, #06B6D4, #10B981)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>Tu Negocio</span>.
                  </>
                ) : (
                  <>
                    Stop Managing <span style={{ background: 'linear-gradient(to right, #EF4444, #F59E0B)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>Chaos</span>.<br/>
                    Start Managing <span style={{ background: 'linear-gradient(135deg, #06B6D4, #10B981)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>Your Business</span>.
                  </>
                )}
              </h1>

              {/* Subheadline */}
              <p style={{ fontSize: '1.25rem', color: '#94A3B8', maxWidth: '32rem', lineHeight: 1.6 }}>
                {language === 'es' ? (
                  <>
                    Un solo sistema para inventario, ventas, contabilidad y nómina.
                    Con <strong style={{ color: 'white' }}>Inteligencia Artificial</strong> que trabaja mientras tú descansas.
                  </>
                ) : (
                  <>
                    One system for inventory, sales, accounting, and payroll.
                    With <strong style={{ color: 'white' }}>AI</strong> that works while you rest.
                  </>
                )}
              </p>

              {/* CTAs */}
              <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
                <div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap' }}>
                  <Link to="/register" style={{ 
                    display: 'inline-flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    gap: '0.5rem',
                    padding: '1rem 2rem',
                    borderRadius: '0.75rem',
                    background: 'linear-gradient(135deg, #06B6D4, #10B981)',
                    color: 'white',
                    fontWeight: 700,
                    fontSize: '1.125rem',
                    textDecoration: 'none',
                    boxShadow: '0 0 40px rgba(6, 182, 212, 0.4)',
                    transition: 'all 0.3s'
                  }}>
                    {t('Empezar Ahora', 'Start Now')}
                    <svg style={{ width: '1.25rem', height: '1.25rem' }} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
                    </svg>
                  </Link>

                  <a href="#demo" style={{ 
                    display: 'inline-flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    gap: '0.5rem',
                    padding: '1rem 2rem',
                    borderRadius: '0.75rem',
                    background: 'transparent',
                    border: '1px solid rgba(255, 255, 255, 0.2)',
                    color: 'white',
                    fontWeight: 600,
                    fontSize: '1.125rem',
                    textDecoration: 'none',
                    transition: 'all 0.3s'
                  }}>
                    <span style={{ width: '2rem', height: '2rem', borderRadius: '50%', background: 'rgba(255,255,255,0.1)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                      <svg style={{ width: '1rem', height: '1rem', fill: 'currentColor' }} viewBox="0 0 24 24">
                        <path d="M8 5v14l11-7z" />
                      </svg>
                    </span>
                    {t('Ver Demo', 'Watch Demo')}
                  </a>
                </div>

                {/* Trust Signals */}
                <div style={{ display: 'flex', alignItems: 'center', gap: '1.5rem', fontSize: '0.875rem', color: '#94A3B8', paddingTop: '1rem', flexWrap: 'wrap' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                    <svg style={{ width: '1.25rem', height: '1.25rem', color: '#10B981' }} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span>{t('Sin tarjeta de crédito', 'No credit card required')}</span>
                  </div>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                    <svg style={{ width: '1.25rem', height: '1.25rem', color: '#10B981' }} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                    </svg>
                    <span>{t('Setup en 15 min', 'Setup in 15 min')}</span>
                  </div>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                    <span style={{ fontSize: '1.125rem' }}>🇻🇪</span>
                    <span>{t('100% Venezuela Ready', '100% Venezuela Ready')}</span>
                  </div>
                </div>
              </div>
            </div>

            {/* Hero Visual (3D Mockup) */}
            {isLargeScreen && (
            <div
              className="perspective-container animate-float"
              style={{
                position: 'relative'
              }}
            >
              {/* Decorative Elements behind */}
              <div style={{
                position: 'absolute',
                top: '-5rem',
                right: '-5rem',
                width: '18rem',
                height: '18rem',
                background: 'linear-gradient(to bottom right, rgba(6,182,212,0.3), rgba(168,85,247,0.3))',
                borderRadius: '50%',
                filter: 'blur(3rem)'
              }} />

              {/* Main Dashboard Card */}
              <div
                className="glass rotate-y-12 hover-rotate-0"
                style={{
                  background: 'rgba(10, 15, 28, 0.8)',
                  backdropFilter: 'blur(12px)',
                  borderRadius: '1rem',
                  padding: '1rem',
                  boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)',
                  border: '1px solid rgba(255,255,255,0.1)',
                  position: 'relative',
                  zIndex: 20,
                  transition: 'transform 700ms ease-out'
                }}
              >
                {/* Mockup Header */}
                <div style={{ display: 'flex', gap: '0.5rem', marginBottom: '1rem', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '0.5rem' }}>
                  <div style={{ width: '0.75rem', height: '0.75rem', borderRadius: '50%', background: '#EF4444' }} />
                  <div style={{ width: '0.75rem', height: '0.75rem', borderRadius: '50%', background: '#F59E0B' }} />
                  <div style={{ width: '0.75rem', height: '0.75rem', borderRadius: '50%', background: '#10B981' }} />
                </div>

                {/* Mockup Body */}
                <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
                  {/* Stats Row */}
                  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '1rem' }}>
                    <div style={{ background: 'rgba(255,255,255,0.05)', borderRadius: '0.5rem', padding: '0.75rem', border: '1px solid rgba(255,255,255,0.05)' }}>
                      <div style={{ fontSize: '0.75rem', color: '#9CA3AF' }}>
                        {t('Ventas Hoy', 'Sales Today')}
                      </div>
                      <div style={{ fontSize: '1.25rem', fontFamily: 'monospace', fontWeight: 700, color: '#10B981' }}>$2,450.00</div>
                      <div style={{ fontSize: '0.75rem', color: '#10B981' }}>↗ 12%</div>
                    </div>
                    <div style={{ background: 'rgba(255,255,255,0.05)', borderRadius: '0.5rem', padding: '0.75rem', border: '1px solid rgba(255,255,255,0.05)' }}>
                      <div style={{ fontSize: '0.75rem', color: '#9CA3AF' }}>
                        {t('Órdenes', 'Orders')}
                      </div>
                      <div style={{ fontSize: '1.25rem', fontFamily: 'monospace', fontWeight: 700, color: 'white' }}>48</div>
                      <div style={{ fontSize: '0.75rem', color: '#6B7280' }}>
                        {t('3 pendientes', '3 pending')}
                      </div>
                    </div>
                    <div style={{ background: 'rgba(255,255,255,0.05)', borderRadius: '0.5rem', padding: '0.75rem', border: '1px solid rgba(255,255,255,0.05)' }}>
                      <div style={{ fontSize: '0.75rem', color: '#9CA3AF' }}>
                        {t('Inventario', 'Inventory')}
                      </div>
                      <div style={{ fontSize: '1.25rem', fontFamily: 'monospace', fontWeight: 700, color: '#F59E0B' }}>Low</div>
                      <div style={{ fontSize: '0.75rem', color: '#F59E0B' }}>
                        {t('5 alertas', '5 alerts')}
                      </div>
                    </div>
                  </div>

                  {/* Graph Area Placeholder */}
                  <div style={{ 
                    height: '8rem',
                    background: 'linear-gradient(to top, rgba(6,182,212,0.1), transparent)',
                    borderRadius: '0.5rem',
                    border: '1px solid rgba(255,255,255,0.05)',
                    position: 'relative',
                    overflow: 'hidden',
                    display: 'flex',
                    alignItems: 'flex-end',
                    padding: '0.5rem',
                    gap: '0.25rem'
                  }}>
                    {/* Bars simulation */}
                    <div style={{ width: '16.67%', background: 'rgba(6,182,212,0.4)', height: '40%', borderRadius: '0.125rem 0.125rem 0 0' }} />
                    <div style={{ width: '16.67%', background: 'rgba(6,182,212,0.5)', height: '60%', borderRadius: '0.125rem 0.125rem 0 0' }} />
                    <div style={{ width: '16.67%', background: 'rgba(6,182,212,0.6)', height: '30%', borderRadius: '0.125rem 0.125rem 0 0' }} />
                    <div style={{ width: '16.67%', background: 'rgba(6,182,212,0.7)', height: '80%', borderRadius: '0.125rem 0.125rem 0 0' }} />
                    <div style={{ width: '16.67%', background: 'rgba(6,182,212,0.8)', height: '55%', borderRadius: '0.125rem 0.125rem 0 0' }} />
                    <div style={{ width: '16.67%', background: '#10B981', height: '90%', borderRadius: '0.125rem 0.125rem 0 0', boxShadow: '0 0 15px rgba(16,185,129,0.5)' }} />
                  </div>

                  {/* Floating Notification */}
                  <div
                    className="glass animate-bounce-custom"
                    style={{
                      position: 'absolute',
                      right: '-3rem',
                      top: '5rem',
                      padding: '0.75rem',
                      borderRadius: '0.75rem',
                      borderLeft: '4px solid #10B981',
                      boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)'
                    }}
                  >
                    <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
                      <div style={{ width: '2rem', height: '2rem', borderRadius: '50%', background: 'rgba(16,185,129,0.2)', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#10B981' }}>
                        <svg style={{ width: '1rem', height: '1rem' }} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                      </div>
                      <div>
                        <div style={{ fontSize: '0.75rem', fontWeight: 700, color: 'white' }}>
                          {t('Nueva Venta #1024', 'New Sale #1024')}
                        </div>
                        <div style={{ fontSize: '0.75rem', color: '#9CA3AF' }}>
                          {t('Hace 2 min • $150.00', '2 min ago • $150.00')}
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            )}

          </div>
        </div>
      </section>

      {/* SECTION 2: LA BARRA DE DOLOR (From V2) */}
      <section id="section-2-parallax" style={{
        paddingTop: isLargeScreen ? '12rem' : '8rem',
        paddingBottom: isLargeScreen ? '12rem' : '8rem',
        paddingLeft: '1rem',
        paddingRight: '1rem',
        background: 'rgba(0,0,0,0.3)',
        position: 'relative',
        overflow: 'hidden'
      }}>
        {/* Light Rays Background - Parallax Layer 1 (slowest) */}
        <div id="light-rays-container" className="parallax-bg" data-speed="0.3" style={{
          position: 'absolute',
          inset: 0,
          opacity: 0.2,
          pointerEvents: 'none'
        }}>
          <LightRays options={{
            raysOrigin: 'top-center',
            raysColor: '#ffffff',
            raysSpeed: 0.8,
            lightSpread: 0.2,
            rayLength: 1.5,
            pulsating: true,
            fadeDistance: 1.0,
            saturation: 1.0,
            followMouse: true,
            mouseInfluence: 0.1,
            noiseAmount: 0.0,
            distortion: 0.0
          }} />
        </div>

        <div style={{ maxWidth: '80rem', margin: '0 auto', position: 'relative', zIndex: 10 }}>

          {/* Headline - Parallax Layer 2 (faster) */}
          <div className="parallax-headline" data-speed="0.15" style={{
            textAlign: 'center',
            marginBottom: '4rem'
          }}>
            <h2 style={{
              fontSize: isLargeScreen ? '3rem' : '2.25rem',
              fontWeight: 700,
              marginBottom: '1rem'
            }}>
              <span className={language === 'es' ? '' : 'hidden'}>¿Te suena familiar?</span>
              <span className={language === 'en' ? '' : 'hidden'}>Sound familiar?</span>
            </h2>
          </div>

          {/* Pain Points Grid - Parallax Layer 3 (normal) */}
          <div className="parallax-cards" data-speed="0.05" style={{
            display: 'grid',
            gridTemplateColumns: isLargeScreen ? 'repeat(3, 1fr)' : (window.innerWidth >= 768 ? 'repeat(2, 1fr)' : '1fr'),
            gap: '1.5rem'
          }}>

            {/* Pain 1 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>📊</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Uso Excel, QuickBooks, WhatsApp Business, y 3 apps más... y ninguna habla con la otra."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "I use Excel, QuickBooks, WhatsApp Business, and 3 other apps... and none of them talk to each other."
              </p>
            </div>

            {/* Pain 2 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>📦</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Se me venden productos que ya no tengo. O se vencen porque nadie me avisó."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "I sell products I don't have. Or they expire because no one warned me."
              </p>
            </div>

            {/* Pain 3 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>💸</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Gasto más de $800/mes en suscripciones y todavía no sé si gano o pierdo dinero."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "I spend $800+/month on subscriptions and still don't know if I'm making or losing money."
              </p>
            </div>

            {/* Pain 4 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>⏰</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Paso 3 horas al día contestando WhatsApp. No puedo ni almorzar tranquilo."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "I spend 3 hours a day answering WhatsApp. I can't even have lunch in peace."
              </p>
            </div>

            {/* Pain 5 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>📱</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Quiero vender online pero no tengo tiempo de manejar otra plataforma."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "I want to sell online but don't have time to manage another platform."
              </p>
            </div>

            {/* Pain 6 */}
            <div className="glass-card group" style={{
              borderRadius: '1rem',
              padding: '1.5rem',
              border: '1px solid rgba(251,191,36,0.2)',
              transition: 'all 0.3s'
            }}>
              <div style={{ fontSize: '2.25rem', marginBottom: '1rem' }}>😓</div>
              <p className={language === 'es' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Los reportes para el contador me toman un día entero. Cada mes."
              </p>
              <p className={language === 'en' ? '' : 'hidden'} style={{
                color: '#94A3B8',
                lineHeight: 1.625
              }}>
                "Monthly reports for my accountant take an entire day. Every. Single. Month."
              </p>
            </div>

          </div>

          {/* Closing Statement */}
          <div style={{ textAlign: 'center', marginTop: '3rem' }}>
            <p style={{ fontSize: '1.25rem', color: '#94A3B8' }}>
              <span className={language === 'es' ? '' : 'hidden'}>
                Si marcaste 2 o más... no es tu culpa. <span style={{ color: 'white', fontWeight: 700 }}>Tu negocio creció, tus herramientas no.</span>
              </span>
              <span className={language === 'en' ? '' : 'hidden'}>
                If you checked 2 or more... it's not your fault. <span style={{ color: 'white', fontWeight: 700 }}>Your business grew. Your tools didn't.</span>
              </span>
            </p>
          </div>

        </div>
      </section>

      {/* SECTION 3: SCROLL STACK (Vanilla JS Implementation) */}
      <section id="section-scroll-stack" className="bg-navy-900" style={{
        position: 'relative',
        height: '250vh'
      }}>
        {/* Height defines scroll duration */}
        <div style={{
          position: 'sticky',
          top: 0,
          height: '100vh',
          width: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          overflow: 'hidden'
        }}>

          {/* Dynamic Background */}
          <div style={{
            position: 'absolute',
            inset: 0,
            pointerEvents: 'none'
          }}>
            <div className="animate-pulse-slow" style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              width: '800px',
              height: '800px',
              background: 'rgba(22, 78, 99, 0.2)',
              filter: 'blur(120px)',
              borderRadius: '50%',
              mixBlendMode: 'screen'
            }} />
          </div>

          {/* Bottom Mask (Blur effect for entering cards) */}
          <div style={{
            position: 'absolute',
            bottom: 0,
            left: 0,
            width: '100%',
            height: isLargeScreen ? '20rem' : '16rem',
            background: 'linear-gradient(to top, #0A0F1C, #0A0F1C, transparent)',
            zIndex: 20,
            pointerEvents: 'none'
          }} />

          <div style={{
            position: 'relative',
            zIndex: 10,
            width: '100%',
            maxWidth: '80rem',
            padding: '0 1rem',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            height: '100%',
            justifyContent: 'center'
          }}>

            {/* Initial Title */}
            <h3 id="stack-title" style={{
              fontSize: isLargeScreen ? '3rem' : '1.875rem',
              fontWeight: 700,
              color: 'white',
              marginBottom: '1rem',
              letterSpacing: '0.025em',
              transition: 'all 700ms',
              transform: 'translateY(0)',
              filter: 'drop-shadow(0 10px 8px rgba(0, 0, 0, 0.04)) drop-shadow(0 4px 3px rgba(0, 0, 0, 0.1))'
            }}>
              <span className={language === 'es' ? '' : 'hidden'}>Imagina un lunes donde...</span>
              <span className={language === 'en' ? '' : 'hidden'}>Imagine a Monday where...</span>
            </h3>

            {/* Cards Container */}
            <div style={{
              position: 'relative',
              width: '100%',
              maxWidth: '42rem',
              height: isLargeScreen ? '300px' : '400px',
              perspective: '1000px'
            }}>
              {/* Cards will be controlled by JS */}

              {/* Card 1 */}
              <div className="stack-card glass-card" style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                padding: '2rem',
                borderRadius: '1.5rem',
                border: '1px solid rgba(255,255,255,0.1)',
                boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)',
                transformOrigin: 'top',
                background: 'rgba(10, 15, 28, 0.8)'
              }}>
                <div style={{ display: 'flex', alignItems: 'flex-start', gap: '1.5rem' }}>
                  <div style={{
                    width: '3.5rem',
                    height: '3.5rem',
                    borderRadius: '1rem',
                    background: 'rgba(16, 185, 129, 0.2)',
                    color: '#10B981',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    flexShrink: 0,
                    fontSize: '1.875rem',
                    boxShadow: '0 0 15px rgba(16, 185, 129, 0.3)'
                  }}>
                    ✓
                  </div>
                  <div>
                    <p style={{
                      fontSize: isLargeScreen ? '1.5rem' : '1.25rem',
                      color: '#E5E7EB',
                      lineHeight: 1.625,
                      fontWeight: 500
                    }}>
                      <span className={language === 'es' ? '' : 'hidden'}>
                        Abres <span style={{ color: 'white', fontWeight: 700 }}>UNA sola app</span> y ves todo tu negocio. Adiós al caos.
                      </span>
                      <span className={language === 'en' ? '' : 'hidden'}>
                        You open <span style={{ color: 'white', fontWeight: 700 }}>ONE app</span> and see your entire business. Goodbye chaos.
                      </span>
                    </p>
                  </div>
                </div>
              </div>

              {/* Card 2 */}
              <div className="stack-card glass-card" style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                padding: '2rem',
                borderRadius: '1.5rem',
                border: '1px solid rgba(255,255,255,0.1)',
                boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)',
                transformOrigin: 'top',
                background: 'rgba(10, 15, 28, 0.85)'
              }}>
                <div style={{ display: 'flex', alignItems: 'flex-start', gap: '1.5rem' }}>
                  <div style={{
                    width: '3.5rem',
                    height: '3.5rem',
                    borderRadius: '1rem',
                    background: 'rgba(6, 182, 212, 0.2)',
                    color: '#06B6D4',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    flexShrink: 0,
                    fontSize: '1.875rem',
                    boxShadow: '0 0 15px rgba(6, 182, 212, 0.3)'
                  }}>
                    🤖
                  </div>
                  <div>
                    <p style={{
                      fontSize: isLargeScreen ? '1.5rem' : '1.25rem',
                      color: '#E5E7EB',
                      lineHeight: 1.625,
                      fontWeight: 500
                    }}>
                      <span className={language === 'es' ? '' : 'hidden'}>
                        Tu WhatsApp responde solo a las 2am. <br /><span style={{ color: '#06B6D4', fontSize: '1.125rem', fontStyle: 'italic' }}>"Hola, aquí tienes nuestro menú..."</span>
                      </span>
                      <span className={language === 'en' ? '' : 'hidden'}>
                        Your WhatsApp answers by itself at 2am. <br /><span style={{ color: '#06B6D4', fontSize: '1.125rem', fontStyle: 'italic' }}>"Hi, here is our menu..."</span>
                      </span>
                    </p>
                  </div>
                </div>
              </div>

              {/* Card 3 */}
              <div className="stack-card glass-card" style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                padding: '2rem',
                borderRadius: '1.5rem',
                border: '1px solid rgba(255,255,255,0.1)',
                boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)',
                transformOrigin: 'top',
                background: 'rgba(10, 15, 28, 0.9)'
              }}>
                <div style={{ display: 'flex', alignItems: 'flex-start', gap: '1.5rem' }}>
                  <div style={{
                    width: '3.5rem',
                    height: '3.5rem',
                    borderRadius: '1rem',
                    background: 'rgba(168, 85, 247, 0.2)',
                    color: '#A855F7',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    flexShrink: 0,
                    fontSize: '1.875rem',
                    boxShadow: '0 0 15px rgba(168, 85, 247, 0.3)'
                  }}>
                    💰
                  </div>
                  <div>
                    <p style={{
                      fontSize: isLargeScreen ? '1.5rem' : '1.25rem',
                      color: '#E5E7EB',
                      lineHeight: 1.625,
                      fontWeight: 500
                    }}>
                      <span className={language === 'es' ? '' : 'hidden'}>
                        Sabes exactamente cuánto ganaste esta semana. Al centavo.
                      </span>
                      <span className={language === 'en' ? '' : 'hidden'}>
                        You know exactly how much you made this week. To the penny.
                      </span>
                    </p>
                  </div>
                </div>
              </div>

              {/* Card 4 */}
              <div className="stack-card glass-card" style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                padding: '2rem',
                borderRadius: '1.5rem',
                border: '1px solid rgba(255,255,255,0.1)',
                boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)',
                transformOrigin: 'top',
                background: 'rgba(10, 15, 28, 0.95)'
              }}>
                <div style={{ display: 'flex', alignItems: 'flex-start', gap: '1.5rem' }}>
                  <div style={{
                    width: '3.5rem',
                    height: '3.5rem',
                    borderRadius: '1rem',
                    background: 'rgba(245, 158, 11, 0.2)',
                    color: '#F59E0B',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    flexShrink: 0,
                    fontSize: '1.875rem',
                    boxShadow: '0 0 15px rgba(245, 158, 11, 0.3)'
                  }}>
                    📦
                  </div>
                  <div>
                    <p style={{
                      fontSize: isLargeScreen ? '1.5rem' : '1.25rem',
                      color: '#E5E7EB',
                      lineHeight: 1.625,
                      fontWeight: 500
                    }}>
                      <span className={language === 'es' ? '' : 'hidden'}>
                        Tu inventario se actualiza con cada venta.
                      </span>
                      <span className={language === 'en' ? '' : 'hidden'}>
                        Updates with every sale. Never sell what you don't have again.
                      </span>
                    </p>
                  </div>
                </div>
              </div>

            </div>

            {/* Final Title */}
            <div id="stack-footer" style={{
              marginTop: '2rem',
              opacity: 0,
              transform: 'translateY(40px)',
              transition: 'all 700ms',
              textAlign: 'center'
            }}>
              <div style={{
                position: 'relative',
                display: 'flex',
                justifyContent: 'center',
                marginBottom: '1.5rem'
              }}>
                <span style={{
                  padding: '0.25rem 1rem',
                  borderRadius: '9999px',
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(255, 255, 255, 0.1)',
                  fontSize: '0.875rem',
                  color: '#67E8F9',
                  textTransform: 'uppercase',
                  letterSpacing: '0.1em'
                }}>
                  <span className={language === 'es' ? '' : 'hidden'}>La Solución</span>
                  <span className={language === 'en' ? '' : 'hidden'}>The Solution</span>
                </span>
              </div>
              <h2 className="text-gradient animate-pulse-glow" style={{
                fontSize: isLargeScreen ? '4.5rem' : '3rem',
                fontWeight: 900,
                letterSpacing: '-0.025em'
              }}>
                <span className={language === 'es' ? '' : 'hidden'}>Eso es SmartKubik.</span>
                <span className={language === 'en' ? '' : 'hidden'}>That's SmartKubik.</span>
              </h2>
            </div>

          </div>
        </div>
      </section>

      {/* Placeholder for more sections */}
      <div style={{ padding: '4rem 2rem', textAlign: 'center', color: '#94A3B8' }}>
        <p>Sección 3 completa. Las demás secciones se agregarán una por una.</p>
      </div>

    </div>
  );
};

export default SmartKubikLanding;
